@page "/hidden-warehouse"

<PageTitle>Warehouse — TechHaven Electronics</PageTitle>

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-lg-6 text-center">
            <div class="display-1 mb-3">&#x1F3ED;</div>
            <h2 class="fw-bold">Internal Warehouse Portal</h2>
            <p class="text-muted mb-4">
                This page is for authorised warehouse staff only.<br />
                If you found this by reading <code>robots.txt</code> — well done.
            </p>

            <div class="card border-warning shadow-sm p-4 mb-4">
                <h5 class="text-warning">&#x26A0; Internal Notice</h5>
                <p class="mb-2">
                    This URL was excluded from search engines via <code>robots.txt</code>.<br />
                    Unfortunately, <strong>robots.txt is publicly readable</strong> — it's not a security measure.
                </p>
            </div>

            <div class="alert alert-success">
                <strong>FLAG4: WAREHOUSE_04</strong>
            </div>

            <p class="text-muted small mt-3">
                Lesson: Never rely on <code>robots.txt</code> to hide sensitive URLs.
                Use proper authentication instead.
            </p>
        </div>
    </div>
</div>
